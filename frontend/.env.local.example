# Backend URL for server-side calls (never exposed to client)
BACKEND_URL=http://localhost:8000

# Public backend URL (used for client-side calls if needed)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Cookie settings
COOKIE_SECURE=false
